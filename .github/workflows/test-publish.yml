name: Test publish (testpypi)

on:
  workflow_dispatch:
    inputs:
      package_version:
        description: package version
        required: true

jobs:
  publish:
    runs-on: ubuntu-latest
    permissions:
      id-token: write
    environment:
      name: testpypi
    defaults:
      run:
        shell: bash
    steps:
      - uses: actions/checkout@v7
      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.12"
      - name: Install uv
        run: |
          pip install -r requirements.setup.txt
      - name: Test publish
        env:
          PACKAGE_VERSION: "${{ inputs.package_version }}"
        run: |
          bash dev/publish.sh testpypi

  test-published-package:
    needs:
      - publish
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ["3.10", "3.11", "3.12"]
    defaults:
      run:
        shell: bash
    env:
      PYTHON_PACKAGE_NAME: bq-inspector
      PYTHON_NAME_SPACE: bq_inspector
    steps:
      - uses: actions/checkout@v7
      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: "${{ matrix.python-version }}"
      - name: Install packages
        env:
          PACKAGE_VERSION: "${{ inputs.package_version }}"
        run: |
          python3 -m pip install -U pip
          python3 -m pip install \
            --index-url https://test.pypi.org/simple/ \
            --extra-index-url https://pypi.org/simple/ \
            --force-reinstall \
            -U "${PYTHON_PACKAGE_NAME}==${PACKAGE_VERSION}"
      # It takes some time when the package gets available.
      - name: Sleep
        run: |
          sleep 30
      - name: Test package
        run: |
          python -c "import ${PYTHON_NAME_SPACE}"
